import express from 'express';import { createProxyMiddleware } from 'http-proxy-middleware';import http from 'http';import assert from 'assert';async function run() {  const canonicalApp = express();  canonicalApp.use(express.json());    let targetRequestLog: any = null;    canonicalApp.all('*all', (req, res) => {    targetRequestLog = {      method: req.method,      path: req.path,      query: req.query,      headers: req.headers,      body: req.body    };    if (req.path === '/api/error500') return res.status(500).json({ error: 'Server error' });    if (req.path === '/api/error401') return res.status(401).json({ error: 'Unauthorized' });    if (req.path === '/api/error403') return res.status(403).json({ error: 'Forbidden' });    res.json({ message: 'Success from canonical' });  });  const canonicalServer = canonicalApp.listen(0);  const targetPort = (canonicalServer.address() as any).port;  const canonicalOrigin = `http://localhost:${targetPort}`;  const proxyApp = express();    proxyApp.use('/api', createProxyMiddleware({    target: canonicalOrigin,    changeOrigin: true,    ws: true,    proxyTimeout: 15000,    on: {      proxyReq: (proxyReq, req, res) => {        proxyReq.removeHeader('cookie');        proxyReq.removeHeader('x-forwarded-host');        proxyReq.removeHeader('x-forwarded-proto');        proxyReq.removeHeader('host');        proxyReq.setHeader('X-MusicScale-Client-Environment', 'ai-studio-preview');      }    }  }));  const proxyServer = proxyApp.listen(0);  const proxyPort = (proxyServer.address() as any).port;  const proxyUrl = `http://localhost:${proxyPort}`;  try {    let res = await fetch(`${proxyUrl}/api/test?a=1`);    console.log("Status: ", res.status, await res.text());  } finally {    canonicalServer.close();    proxyServer.close();  }}run();
